services:
  sotto:
    build: ./server
    ports:
      - "8377:8377"
    volumes:
      - sotto-data:/data/sotto
      - ./server/config.yaml.example:/data/sotto/config.yaml:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  sotto-data:
